<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Productos</title>
    <!-- Agrega el enlace al CDN de Bootstrap para usar sus estilos -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 20px; /* Agrega un espacio alrededor del contenido para mayor legibilidad */
        }

        .product {
            border: 1px solid #ddd;
            padding: 10px;
            margin-bottom: 20px;
        }

        /* Personaliza según tus necesidades */
        .welcome-message {
            background-color: #e6f7ff;
            padding: 10px;
            margin-bottom: 20px;
        }

        .product-image {
            max-width: 100%;
            height: auto;
        }
        .paginacion{
            display: flex;
            justify-content: center;
        }
    </style>
</head>

<body>
    <div class="container">
        {{#if user}}
        <div class="welcome-message">
            <h3>Bienvenido, {{user.first_name}}! Para ver todos tus datos o cerrar sesión ingresa <a
                    href="/userProfile">aquí</a>.</h3>
        </div>
        {{else}}
        <div class="welcome-message">
            <h2>Inicia sesión para recomendarte lo mejor para ti! <a href="/login">Iniciar Sesión</a></h2>
        </div>
        {{/if}}

        <h1>Lista de Productos</h1>

        <div class="row row-cols-1 row-cols-md-3 g-4">
            {{#each products}}
            <div class="col mb-4">
                <div class="card product">
                    <img src="https://dummyimage.com/200x100/616161/ffffff.png&text=example" alt="{{this._doc.title}}" class="product-image">

                    <div class="card-body">
                        <h5 class="card-title">{{this._doc.title}}</h5>
                        <p class="card-text">{{this._doc.description}}</p>
                        <p class="card-text">Precio: {{this._doc.price}}</p>
                        <p class="card-text">Stock: {{this._doc.stock}}</p>
                        <p class="card-text">Categoría: {{this._doc.category}}</p>

                        <!-- Agregar al carrito directamente -->
                        <form action="/api/carts/:cid/products/{{_id}}" method="post">
                            <button type="submit" class="btn btn-primary">Agregar al Carrito</button>
                        </form>
                        <br>

                        <!-- Ver detalles del producto -->
                        <a href="/products/{{_uid}}" class="btn btn-secondary">Ver Detalles</a>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>

        <!-- Paginación -->
        <div class="mt-3">
            {{#if hasPrevPage}}
            <a href="{{prevLink}}" class="btn btn-outline-primary">Página anterior</a>
            {{/if}}

            <div class="paginacion">Página {{page}} de {{totalPages}}</div>

            {{#if hasNextPage}}
            <a href="{{nextLink}}" class="btn btn-outline-primary">Página siguiente</a>
            {{/if}}
        </div>
    </div>

    <!-- Agrega el script de Bootstrap para funcionalidades adicionales (opcional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/helpers.js"></script>
</body>

</html>
